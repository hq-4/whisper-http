# Requirements Clarification – Round v4

Nearly there. A few final areas to close before implementation:

1. **API contract details**
   - What HTTP endpoint/verb should we expose for transcript requests (e.g., `POST /transcribe`)? - POST /transcribe
   - How will the client supply the audio path—JSON body field, query param, or other? Please specify the exact request schema we should validate against. - JSON body field with audio path only
   - Should the response wrap the transcript JSON payload inside an envelope (e.g., `{ "data": { ... } }`) or return the transcript object directly? - return the transcript object directly
2. **Security & validation**
   - Since the client passes filesystem paths, do we need to enforce directory allow-lists or sandbox checks to prevent accidental access outside approved roots? - no
   - Any authentication/authorization required, or is the service trusted within the internal network? - trusted within the internal network
3. **Process execution & resource usage**
   - You requested we rely on `subprocess`; should we execute the diarization repo via its CLI entrypoint, or invoke a Python module with `uv run python -m ...`? Please confirm the expected command signature. - via its CLI entrypoint (python diarize.py -a audio.mp3)
   - Are there constraints on GPU usage we should respect (e.g., force CPU fallback for faster-whisper, or allow it to auto-detect)? - allow it to auto-detect
4. **Testing expectations**
   - Beyond unit tests for schema conversion and fallback logic, do you need integration tests that execute the real diarization submodule (possibly behind a feature flag), or is mocking subprocess output sufficient? - integration tests that execute the real diarization submodule
   - Should we commit fixtures/sample audio files (small) for automated tests, or keep them external? - commit fixtures/sample audio files
5. **Documentation & reproducibility**
   - Where should we record the pinned submodule commit hash (e.g., `docs/SUBMODULE_VERSION.md`, `IMPLEMENTATION_PLAN.md`, or `.gitmodules` comments)? - .gitmodules
   - Do you need a runbook in `docs/` detailing startup steps (`uv sync`, `git submodule update`, `.env` setup), or will inline README updates suffice? - inline README updates

Let me know, and I’ll roll the answers into the final plan update.
