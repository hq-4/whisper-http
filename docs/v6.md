# Requirements Clarification – Round v6

Tying off the last decision points before we freeze the spec:

1. **Integration test cadence**
   - We can either: (a) run long-haul diarization integration tests by default with `uv run -m pytest -q`, increasing routine test time to several minutes, or (b) guard them with a `@pytest.mark.integration` and only execute when explicitly requested via `uv run -m pytest -m integration`. Which workflow do you prefer? - (a). i have also cut a 2 minute sample from the high qualify file so it should take less time to process. at some point i will also do the same for the low quality file.
2. **Diarization submodule customization**
   - Would you rather keep the upstream CLI untouched and build our own wrapper around it, or are you open to forking/modifying selected scripts (e.g., `diarize.py`) inside the submodule to better align with our server use case, with changes tracked in our repo? - please keep the cli untouched. i did not realize it would create more difficulty if we modified it.
3. **Dependency installation strategy**
   - Given the upstream guidance `pip install -c constraints.txt -r requirements.txt`, do you want us to reproduce this via UV (e.g., `uv pip install ...`) during setup, or delegate all third-party installs to the submodule’s Makefile/scripts while our top-level project only manages application dependencies? - please use uv since it is a dropin replacement. 

Let me know your preferences and we’ll finalize the implementation plan.
