# Requirements Clarification – Round v5

Last round of confirmations before we lock the spec:

1. **Segment timing & metadata format**
   - Should timestamps be emitted as strings in `HH:MM:SS.mmm` (mirroring SRT) or as floating-point seconds? If strings, confirm millisecond precision. - floating point seconds
   - For `total_duration`, do you expect the same unit/format as segment timestamps, or is a numeric second count preferred? - numeric second count
   - How should `speaker_count` be defined—unique speakers detected in the successful transcript, or derived from diarization attempt even if fallback succeeds? - set as zero if diarization fails
2. **Fallback pipeline configuration**
   - Which faster-whisper model variant should we default to when invoked (e.g., `large-v3` matching the whitelist)? - large-v3
   - If faster-whisper succeeds, should we still persist any artifacts it produces (e.g., generated SRT) alongside the JSON, or is JSON-only acceptable? - JSON-only
3. **Integration test execution**
   - Do you want the integration tests to run by default under `uv run -m pytest -q`, or gated behind a marker (e.g., `pytest -m integration`) to avoid long runtimes unless explicitly requested? - what is the difference? i am gonna put 1 high quality audio that i know passes diarization and 1 that i know fails diarization.
   - Any maximum duration expectation for these integration tests so we can size the fixture audio appropriately? - 10 mins for any test that touches the diar pipeline in full. 
4. **Sample fixtures**
   - Provide guidance on acceptable fixture duration/size (e.g., ≤30 seconds, specific sample rate) so we stay within repo size norms. - the sample files will be 40-60 mins long, but my gpu will take roughly 3.5 mins to run. so lets say 10 mins for the fixture files.
   - Should fixtures live under `tests/fixtures/audio/` or another preferred directory? - tests/fixtures/audio/
5. **Operational runbook notes**
   - Besides README updates, do you want us to include a checklist in `IMPLEMENTATION_PLAN.md` summarizing deployment prerequisites (uv env activation, `.env`, submodule sync), or is README alone sufficient? - README
   - Any expectations for documenting how to update the pinned submodule hash when upstream releases changes? - README, but for now i will only use that hash in that point in time.

Once these are answered, we’ll finalize the plan and proceed to implementation steps.
