# Requirements Clarification – Round v2

Building on your v1 answers, here are the next items to nail down:

1. **Transcript JSON schema refinement**
   - When converting lines like `Speaker 2: Wells Fargo seeks…`, should we split the `Speaker 2` label into a dedicated `speaker` field and strip it from the caption text, or keep it inline? - what would be the difference? ultimately i want a json schema that is easy for me to read, but also easier for an llm to parse via ollama.
   - Beyond timestamps and text, do you want per-segment identifiers (e.g., sequence numbers, UUIDs) or aggregate metadata (total duration, speaker roster, etc.) in the JSON payload? - yes include sequence numbers and only metadata. there is no way to understand which speaker's name is which other than Speaker 1, Speaker 2, etc. that is left to the llm to understand.
   - For the top-level structure, should we return a single JSON object (with segments array + metadata) or stream multiple objects per segment? - return a single json object. make sure it is filename.json and not filename.jsonl.
2. **Fallback provenance & reporting**
   - Should the JSON include both the primary diarization output (if partial) and the fallback transcript, or only the successful final result with a provenance flag? - i understand the diarzation is a simple pass/fail and there is no partial output. if it fails you need to fallback to faster-whisper. if that fails you need to return an error where the type is "diarization" and the message is "diarization failed".
   - If the fallback fires, do we need to surface the diarization failure reason/message anywhere in the response or logs? - yes
3. **Status/progress communication**
   - Would a simple synchronous HTTP response (submit request → wait → receive JSON) meet the client’s needs, or do you envision polling/webhooks to track long-running jobs? - i want a simple synchronous HTTP response (submit request → wait → receive JSON) and i expect the request to last less than 10 minutes.
   - If synchronous is acceptable, is there a maximum tolerable processing time before we should send an interim heartbeat or timeout? - 10 minutes and add that toggle to .env
4. **Configuration surface**
   - For `.env` controls (language/model size), should we enforce strict validation (e.g., whitelist of model identifiers) or allow pass-through values that propagate directly to the underlying libraries? - i want to enforce strict validation (e.g., whitelist of model identifiers)
   - Are there other knobs we should expose (beam size, temperature, vad/energy thresholds) for either pipeline? - no
5. **Operational hygiene**
   - Do we need to archive the generated SRT/JSON files or clean them up automatically after serving the response? - no
   - Any audit/logging requirements beyond the standard dual-sink logging mandated by our global rules (e.g., per-request request/response summaries, latency metrics)? - no

Let me know your thoughts and we’ll iterate further.
