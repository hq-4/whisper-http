# Requirements Clarification – Round v1

Please help clarify the following so we can shape the server behavior precisely:

1. **Audio ingestion workflow**
   - How will clients submit audio? (e.g., file upload via HTTP POST, pre-signed URL, shared volume path) - it is a single client that will just pass the path to the audio file. hopefully (you need to do tests to confirm this) the outputs of the diarization are stored in the same directory as the audio file and NOT where the client is running.
   - Are there constraints on accepted formats, duration, or size that we must enforce? - no
2. **SRT → JSON transformation expectations**
   - What specific fields and structure should the resulting JSON include (timestamps, speaker IDs, confidence scores, etc.)? - @2024-12-13 - Tim Latimer on Solving the Financing Problem for Geothermal.srt here is a sample srt from the diarzation repo. please ask me for clarification if you need it.
   - Should we include raw captions alongside any enriched metadata? - we need to include a json key that explicitly says if it is using diarization or faster-whisper.
3. **Failure handling & fallback logic**
   - Which exceptions or failure signals from the diarization repo should trigger the fallback to `faster-whisper`? - we have to test first but i have seen in stdout that it fails catastrophically.
   - Do we need retry/backoff or simply a single fallback attempt per request? - no retries or backoffs. what has to happen is if the diarization fails, we should fallback to faster-whisper.
4. **Operational constraints**
   - Are there concurrency limits or service-level expectations (max simultaneous requests, latency targets)? - there will only be 1 client (on the same bare metal server as the diarization repo). so no concurrency limits.
   - How should we report progress or status to clients while the blocking thread runs? - i don't know.
5. **Environment & deployment**
   - Will the server run on dedicated hardware (GPU/CPU specs) that might influence model selection or resource management? - yes. 32gb, 16 cores, 3080 10gb.
   - Are there configuration knobs (model size, language settings) that need to be runtime-configurable? - yes. language and model size on the .env

Feel free to annotate or answer inline; I will incorporate your feedback into the next iteration.
