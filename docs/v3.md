# Requirements Clarification – Round v3

Thanks for the additional detail. A few closing gaps to make sure the design lines up with your expectations:

1. **Speaker label handling**
   - You noted the goal is readability for you and easy parsing by Ollama. Would you prefer we:
     1. Extract `speaker` as its own field (e.g., `speaker: "Speaker 2"`) and keep the caption text free of the prefix, or
     2. Leave the raw string intact (e.g., `text: "Speaker 2: …"`), possibly duplicating the label in a separate field for machine parsing?
   - If option (1), should we preserve the literal label (“Speaker 2”) or map to numeric IDs (e.g., `speaker_index: 2`) as well? 1. please do (e.g., `speaker: "Speaker 2"`) so it is easy to parse by ollama and it can estimate who the speakers are to later subsitute it at some point.
2. **Metadata and top-level JSON layout**
   - Beyond `segments: []` and basic file info, which aggregate metadata do you want included (e.g., `total_duration`, `speaker_count`, `pipeline_used`, `processing_time_ms`)? - just total duration and speaker count
   - Do you want the provenance flag (`"diarization" | "faster_whisper"`) duplicated both at the top level and per segment, or is a single top-level field sufficient? - top field is fine.
3. **Error contract & logging**
   - For the error response `{type: "diarization", message: "diarization failed"}`, should we include an additional `detail` field containing the underlying exception message, or reserve that for logs only? - logs only
   - When logging failures, do you want the stack trace captured with RichHandler (as per global rules) plus the original stderr/stdout from the diarization process, or just the summarized exception text? - the first one
4. **Environment configuration specifics**
   - Please enumerate the allowed values for the language and model-size environment variables (for both diarization repo and faster-whisper if they differ) so we can codify the whitelist. - turbo and large-v3
   - For the 10-minute timeout toggle, should we expose both a default and a per-request override (query/body param), or keep it strictly controlled via `.env`? - just on .env
5. **Git submodule management**
   - Do you expect the server startup to verify the submodule is initialized at boot (and error if not), or will deployment scripts handle `git submodule update --init --recursive` ahead of time? - deployment scripts
   - Should we pin to the absolute latest commit on the repo’s default branch at build time, or to a specific hash recorded in documentation for reproducibility? - pin to the a specific hash recorded in documentation for reproducibility

Let me know and I’ll incorporate the answers into the next revision of the plan.
